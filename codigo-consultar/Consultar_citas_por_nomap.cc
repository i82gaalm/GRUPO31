#include <cstdlib>
#include "paciente.h"

int main(){

  //tiene que buscar el paciente concreto pedido por terminal y que salga todas las citas que tenga el paciente (pensar luego si en un tiempo determinado o no)
  //Tiene que tener en cuenta si o si el nombre y apellido del paciente
  Consultar_paciente(string nombre, string apellido){
  getPaciente();
  printf("Nombre: ||Apellidos: ||Citas del paciente: \n", );
  scanf("%s\n", getPaciente);
  }
}
